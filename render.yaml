services:
  - type: web
    name: taskflow
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 3001
      - key: DB_PATH
        value: /app/data/taskflow.db
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_WHATSAPP_FROM
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
    disk:
      name: taskflow-data
      mountPath: /app/data
      sizeGB: 1
